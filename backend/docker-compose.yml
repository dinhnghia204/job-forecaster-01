version: '3.8'

services:
  backend:
    build: .
    container_name: job_forecaster_backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped